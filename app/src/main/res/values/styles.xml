<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_theme_gravy</item>
        <item name="colorPrimaryDark">@color/main_theme_gravy</item>
        <item name="colorAccent">@color/main_theme_gravy</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="toolbarStyle">@style/MyActionBarStyle</item>
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--新加-->
    <style name="Toolbar_TextAppearance_White">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Toolbar_TextAppearance_Expanded">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.SwipeBack">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="tab_appearance">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AppTheme.NoActionBar.Fullscreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@drawable/ic_screen_default</item>
        <!-- #region /设置全屏属性-->
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--#endregion -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!---用于显示Dialog的主题-->
    <style name="dialog_style" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@null</item>//边框
        <item name="android:windowIsFloating">true</item>//是否悬浮
        <item name="android:windowIsTranslucent">true</item>//半透明
        <item name="android:windowNoTitle">true</item>//没标题栏
        <item name="android:background">@android:color/transparent</item>//背景透明
        <item name="android:windowBackground">@android:color/transparent</item>//背景透明
        <item name="android:backgroundDimEnabled">true</item>//模糊
        <item name="android:backgroundDimAmount">0.6</item>
    </style>
</resources>
